#Создать сценарий, который при выполнении должен получить не менее двух параметров.
#Если количество переданных сценарию параметров ровно два, то на экране появляются их значения,
#в противном случае – вывести имя сценария и требование задать два параметра.

#!/usr/bin/bash

if [[ $3 == "" ]] && [[ $1 != "" ]] && [[ $2 != "" ]]; then
	echo "$1 $2"
else echo "not valid quantity of the passed params. should be 2.";
fi
